<% const path = require('path') %>
<% jsFiles.forEach((file) => { %>
require(<%- JSON.stringify(path.relative(path.dirname(outputPath), file)) %>);
<% }) %>
module.exports = global['webpackExports'];
